name: Scrape 50 States for Ukraine rankings

on:
  schedule:
    # Runs at 7 minutes past every hour (UTC), not exactly on the hour.
    # GitHub's own docs warn that the top of the hour (":00") is the most
    # congested time slot and scheduled runs can be delayed or even
    # silently dropped there. An offset minute avoids that contention.
    - cron: "7 * * * *"
  workflow_dispatch: {}  # lets you trigger a run manually from the Actions tab

permissions:
  contents: write  # needed so the workflow can commit data/ back to the repo

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python3 -m pip install -r requirements.txt

      - name: Run scraper
        run: python3 scrape_rankings.py

      - name: Commit and push updated data
        run: |
          git config user.name "50states-scraper-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Update rankings snapshot [automated]"
            git push
          fi
